# 单张图片几何识别使用说明

## 功能说明

`predict-image`用于给一张包含单个几何物块的RGB照片预测类别。程序会自动完成主体分割、标准化裁剪、棱线拓扑提取、分类和低置信度拒识。

该接口只用于开发和识别验证。普通RGB图片没有深度信息，输出固定为`safe_for_robot=false`，不能直接驱动吸盘或机械臂。

## 环境准备

在项目根目录打开PowerShell：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev]"
```

如果PowerShell禁止执行`Activate.ps1`，可以不激活环境，继续使用下文的`.\.venv\Scripts\python.exe`完整路径。多行命令的反引号`` ` ``必须位于行末，后面不能有空格。

## 最简单的使用方法

只提供一张JPG或PNG图片。默认使用`models/geometry-rgb-morph-color.npz`：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-image `
  "几何测试_2\四棱锥\square_pyramid_batch02_001.jpg"
```

终端会显示：

```text
状态：识别成功 / 已安全拒识
类别：四棱锥 (square_pyramid)
置信度：0.823
依据：accepted
后端：opencv
耗时：模型 45.20 ms，端到端 68.40 ms
机械执行：禁止（单RGB图片仅用于开发识别）
```

`unknown`不是程序错误，而是图片证据不足或类别间隔太小，系统主动拒绝强行分类。

## 保存预测结果

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-image `
  "图片路径.jpg" `
  --output-dir "output\single-image-demo"
```

输出目录包含：

- `result.json`：类别、置信度、拒识原因、物块框和耗时。
- `annotated.jpg`：带检测框和预测文字的原图。
- `normalized.png`：模型实际分析的标准化物块裁剪。
- `mask.png`：主体分割掩膜。

脚本调用时可增加`--json-only`，让终端只输出JSON：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-image `
  "图片路径.jpg" --json-only
```

## 选择模型

当前建议使用覆盖测试1和测试2姿态、采用形态学补线与Lab色块辅助的模型：

```powershell
--model models/geometry-rgb-morph-color.npz
```

严格测试1训练模型为：

```powershell
--model models/geometry-rgb-edges-v3.npz
```

NPZ模型会由`--backend auto`自动识别为OpenCV后端。如需明确指定，可增加`--backend opencv`。

## Python接口

```python
from sorting_vision import GeometryImagePredictor

predictor = GeometryImagePredictor.load(
    "models/geometry-rgb-morph-color.npz"
)
result = predictor.predict_file("图片路径.jpg")

print(result.prediction.label_id)
print(result.prediction.label_name)
print(result.prediction.confidence)
print(result.prediction.accepted)
print(result.prediction.reason)
```

处理已经由OpenCV读取的图片：

```python
import cv2
from sorting_vision import GeometryImagePredictor

image = cv2.imread("图片路径.jpg")
predictor = GeometryImagePredictor.load()
result = predictor.predict(image)
```

## 拍摄要求与常见问题

- 一张图片只放一个主要物块，物块不要超出画面。
- 使用稳定、均匀的漫反射照明，避免强阴影遮住内部棱线。
- 图片可以是1280×720或1920×1080；长边超过1280像素时会先等比例缩小，再生成256×256标准化裁剪。
- `object_not_found`表示没有提取到主体。
- `multiple_objects`表示中央区域检测到多个主体。
- `edge_evidence_low`表示可见内部棱不足。
- `margin_rejected`表示前两种类别过于接近，程序选择安全拒识。

当前棱线提取不使用霍夫变换。开运算用于去除印刷纹理和小色斑，闭运算用于补齐短断线，Lab大色块边界作为分面辅助；证据仍不足时会返回`unknown`。

查看所有参数：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-image --help
```
