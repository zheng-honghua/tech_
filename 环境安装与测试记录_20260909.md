# 单目与双目环境安装和测试记录（2026-09-09）

## 安装范围

两个工程使用互相独立的 Python 3.14.7 虚拟环境，并以 editable 模式安装当前源码。

单目完整安装命令：

```powershell
Set-Location "C:\Users\d114\Desktop\tech_\monocular camera"
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev,realsense,cnn,cnn-train]"
```

双目完整安装命令：

```powershell
Set-Location "C:\Users\d114\Desktop\tech_\stereo camera"
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev,realsense,dual,cnn,cnn-train]"
```

已覆盖基础运行、pytest、RealSense、OpenCV、OpenVINO、ONNX、PyTorch、TorchVision 和 NNCF。两个环境执行 `pip check` 均返回 `No broken requirements found`。

## 关键版本与后端

共同关键版本：

- Python：3.14.7
- NumPy：2.4.6
- OpenCV：5.0.0
- OpenVINO：2026.3.1
- ONNX：1.22.0
- NNCF：3.3.0

单目 PyTorch/TorchVision 为 `2.13.0+cpu / 0.28.0+cpu`；双目为 `2.14.0+cpu / 0.29.0+cpu`。两边张量运算烟雾测试通过。当前是 CPU 版本，不代表已经配置 NVIDIA CUDA 训练环境。OpenVINO 可用设备为 `CPU`。

## 回归结果

由于本机 `C:\Users\d114\AppData\Local\Temp\pytest-of-d114` 存在访问权限异常，直接使用 pytest 默认临时目录会让所有依赖 `tmp_path` 的测试在 setup 阶段报错。这不是算法或依赖失败。统一使用项目内临时目录：

```powershell
python -m pytest -q --basetemp .pytest-tmp
```

最终结果：

- 单目：`150 passed`
- 双目：`174 passed`
- 两边 `pip check`、`compileall`、CLI 帮助入口均通过。
- 双目的 `dual_rgbd_side_capture.py --help` 和 `dual_apriltag_calibrate.py --help` 均正常。
- 双目环境中的 `sorting_vision` 从 `C:\Users\d114\Desktop\tech_\stereo camera\src\sorting_vision` 加载；单目从对应的 `monocular camera\src\sorting_vision` 加载，没有串用工程。

## 硬件运行时检查

RealSense SDK 成功枚举到一台设备：

- 型号：RealSense D435IF
- 序列号：332322072879

本次只进行了 SDK 枚举，没有持续开启相机流，也没有检查侧置 UVC 相机画面、曝光或双机同步。相机实拍、AprilTag 标定和比赛平台识别验收仍需单独执行。
