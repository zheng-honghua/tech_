# 单图多物块几何识别使用说明

## 功能范围

`predict-scene`用于一张RGB图片中包含多个几何物块的情况。程序先分离彼此不接触的物块，再对每个物块独立执行标准化裁剪、内部棱线拓扑提取和类别预测，最后在原图上绘制编号、类别和置信度。

当前接口适合桌面开发测试，不是抓取接口。所有结果固定为`safe_for_robot=false`；没有D415深度时不能生成吸盘三维位姿。

## 直接运行

在项目根目录打开PowerShell：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-scene `
  "C:\图片\多个物块.jpg" `
  --output-dir "output\multi-object"
```

默认加载`models/geometry-rgb-edges-expanded.npz`。指定其他OpenCV棱线模型：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-scene `
  "多个物块.jpg" `
  --backend opencv `
  --model "models\geometry-rgb-edges-v3.npz" `
  --output-dir "output\multi-object-v3"
```

脚本集成时增加`--json-only`，终端将只输出机器可读JSON。

## 输出内容

输出根目录包含：

- `annotated.jpg`：所有物块的编号、框、类别和置信度。
- `foreground-mask.png`：场景前景分割结果。
- `result.json`：物块数量、逐物块预测、坐标框、耗时和产物路径。

每个`object-001/`子目录还包含`normalized.png`、`mask.png`、`edge-map.png`、`topology.png`、`topology.json`和独立`result.json`，可直接检查程序实际使用了哪些棱线。

## Python接口

```python
from sorting_vision import GeometryScenePredictor

predictor = GeometryScenePredictor.load()
result = predictor.predict_file("多个物块.jpg")

for item in result.objects:
    print(
        item.object_id,
        item.prediction.label_name,
        item.prediction.confidence,
        item.prediction.accepted,
    )
```

## 拍摄要求与限制

- 物块之间保留明显背景间隔，不要互相接触、重叠或超出画面。
- 使用单色、干净背景，并避免手、工具头和强投影阴影进入画面。
- 保证物块内部棱线有亮度差；均匀漫反射照明通常更稳定。
- `unknown`表示棱线证据或类别间隔不足，是安全拒识而不是程序崩溃。
- 接触物块当前可能形成一个连通候选，因此不能据此执行机械动作；D415到位后需用深度分割和吸附面验证解决。

查看全部参数：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli predict-scene --help
```
