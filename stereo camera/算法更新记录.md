# 算法更新记录

本文件按实际代码更新时间记录算法变化。每次代码更新必须追加一条记录；不能删除旧结论来掩盖失败实验，也不能把训练回放结果写成独立测试准确率。

## 版本演进基线

以后所有算法更新均在下表基础上顺序追加，从第20阶段开始。既有阶段不得重排、重新分类或覆盖；仅修改文档、测试设施和工程结构时，不占用新的算法阶段编号。

| 更新阶段 | 当时提出的需求 | 这一版采用的方法 | 相比上一版的变化 |
| --- | --- | --- | --- |
| 1．二维分拣初版 | 识别颜色、形状、位置和角度 | 背景差分、Lab颜色识别、形态学处理、轮廓几何、距离变换 | 建立最初的二维识别与抓取点接口 |
| 2．三维视觉框架版 | 物块会侧躺、倾斜，不能只看二维轮廓 | RGB-D输入、托盘平面拟合、点云几何、吸附面分析 | 从二维中心/角度升级到三维位置/法向；当时主要是框架和合成验证 |
| 3．USB/D415与运动互锁版 | 暂时只有USB相机；工具头运动时只预览 | 相机适配器、MOVING/SETTLING/READY状态机、帧差稳定检测 | 分离取流和计算；运动即清除旧结果，RGB禁止抓取 |
| 4．首批几何例图版 | 接入“几何测试_1”的7类34张图 | 主体分割、HOG、Hu矩、轮廓、边缘方向、明暗特征＋距离分类 | 从固定轮廓规则转向可训练的RGB特征模型：`geometry-rgb.npz` |
| 5．OpenCV/CNN双版本 | 想比较传统视觉与CNN | 保留NPZ模型，增加MobileNetV3-Small RGB CNN和OpenVINO接口 | 两套分类器可手动切换，不自动融合 |
| 6．内部棱线拓扑版 | 认为应根据立体物块上的棱判断类别 | CLAHE、Canny、LSD、共线合并、交点、平行/汇聚关系、可见面 | 分类证据从外轮廓扩展到内部结构：`geometry-rgb-edges.npz` |
| 7．测试2与棱线改进版 | 新增图片，继续优化 | 扩充训练样本、过滤外轮廓伪棱、面几何特征、拒识与跨集检查 | 形成edges-v3、expanded、faces等实验模型 |
| 8．单图预测与多物块版 | 希望直接输入图片；一张图里有多个物块 | `predict-image`、`predict-scene`，分割后逐个裁剪分类 | 从开发命令变为直接预测接口，并增加多实例结果输出 |
| 9．开闭运算＋色块辅助版 | 不用霍夫，利用开闭运算和色块补棱 | 形态学开闭、Lab色块划分、色面边界辅助、线段拓扑 | 改善断棱与噪声：`geometry-rgb-morph-color.npz`；未使用霍夫 |
| 10．低噪声点线结构版 | 要轮廓点线尽量干净，并找回内部漏棱 | 多边形拟合、短边剪枝、端点吸附、交点聚类、悬空线删除 | 对提取结果再做结构清理：`geometry-rgb-structure.npz` |
| 11．现有图片CNN训练版 | 传统模型效果差，实际训练CNN看看 | 使用现有图片训练RGB MobileNetV3-Small，加入数据增强 | 实际训练和比较CNN候选；没有变成点线CNN或RGB-D CNN |
| 12．D415实拍与三维结构版 | D415到位，加入深度采集和训练 | 成组保存RGB/深度/内参；三维尺寸、深度分布、局部平面与面拓扑 | 从RGB外观转向真实深度特征，建立RGB-D训练链路 |
| 13．多姿态KNN版 | 新拍不同姿态，提升跨批次识别 | 从每类单质心升级为多姿态KNN，比较各类最近的3个姿态 | 保留同类正放、侧躺、倾斜的不同特征，形成multipose系列 |
| 14．多物体批量测试版 | 批量拍摄整盘场景 | 场景成组采集、组成标签、批量回放、逐实例评测 | 从单件训练扩展到多物块场景测试；数量标签用于评测，不是强制检测出指定数量 |
| 15．白色托盘ROI版 | 只识别托盘内部和边缘 | 托盘ROI检测、RGB支持区域与深度前景联合限制 | 排除托盘外物块，减少背景杂物干扰 |
| 16．新增两轮训练版 | 用新拍摄的多物体场景继续训练测试 | 样本提取、追加多姿态样本、模型对照 | 形成后续multipose-v4等模型；混合场景标签可靠性成为新问题 |
| 17．六棱柱专项优化版 | 六棱柱识别率低 | 统一训练/推理缩放；用公制跨度和近平面占比复核部分八面体预测 | 针对六棱柱侧躺混淆增加受限规则，仍保留v4主模型 |
| 18．视觉审查驱动的分割修正版 | 根据实际图片检查漏检、重复检测 | 小碎片的距离、面积、色相、高度与连接区域约束合并 | 修复同一物块因深度断裂被拆成两件，并保留深度安全拒绝 |
| 19．最新优化建议落实版 | 分离RGB与深度、校准颜色、清理训练来源 | RGB/深度双掩膜、蓝青色标定、歧义标签确认、原始单件独立重训练 | 裁剪与颜色改善；新模型较差未替换，继续使用原v4＋新颜色配置 |

当前推荐链路为：**白色托盘ROI → 深度分割与碎片修复 → 三维几何/可见面拓扑 → 多姿态KNN v4 → 受限几何复核**。RGB完整轮廓用于颜色和展示，真实有效深度用于三维计算和抓取。第19阶段的形状开发回放结果为47/55（85.45%），不是正式比赛准确率。

## 记录格式

```text
## 阶段NN · YYYY-MM-DD：更新名称
- 目的：要解决的问题。
- 算法变化：新增、删除或调整的算法和关键门限；无算法变化时明确写“无”。
- 预期效果：修改前预期改善什么。
- 实际效果：测试集、样本数量、准确率/耗时/拒识率及与旧版对照。
- 视觉审查：实际查看的批次、帧和发现；不适用时说明原因。
- 影响范围：代码、配置和模型文件。
- 已知限制：未解决问题及不能据此宣称的能力。
- 验证：执行过的测试和结果。
```

## 2026-09-05：RGB轮廓与有效深度分离

对应阶段：19。

- 目的：解决D415深度孔洞造成的显示裁剪残缺，同时防止RGB补全被误当作真实抓取面。
- 算法变化：增加受ROI、饱和度、深度实例归属、重叠率和面积倍率约束的RGB连通域恢复；分类继续使用旧深度裁剪，点云和吸附面只使用有效深度掩膜。增加蓝色/青色Lab标定，并将歧义实例改为显式人工确认。
- 预期效果：获得更完整的RGB物块裁剪和更稳定的颜色结果，同时保持三维抓取安全门限。
- 实际效果：multi-02与multi-03共20帧、70个候选；颜色为30蓝、40青，无unknown/black。临时参考标签下，原v4修改前后均为47/55（85.45%）；独立单件重训练候选为40/55（72.73%），因此未替换v4。
- 视觉审查：检查两批20帧RGB/深度总览及70个实例裁剪。确认RGB轮廓改善、深度孔洞未被伪造；仍发现边缘截断和五/六棱柱混淆。multi-02第6–10帧共15个实例保持未确认标签。
- 影响范围：`rgb_instance.py`、`pipeline3d.py`、`types.py`、`cli.py`、颜色配置、训练与审查脚本。
- 已知限制：结果是同批次开发回放，不是正式泛化准确率；低饱和物块的RGB恢复尚未验证；两帧仍因托盘平面漂移被安全拒绝。
- 验证：135项pytest测试通过；compileall与diff检查通过。详细说明见[优化建议落实与使用说明_20260905.md](docs/reports/优化建议落实与使用说明_20260905.md)。

## 2026-09-05：建立强制更新记录规范

- 目的：保证后续每次代码更新都能追溯算法目的和实际效果。
- 算法变化：无，属于工程文档与开发流程更新，不占用阶段编号；下一次算法代码更新从阶段20开始。
- 预期效果：避免模型、门限和实验结果只存在于代码或聊天记录中。
- 实际效果：新增统一记录模板，并在`AGENTS.md`中设为完成条件。
- 视觉审查：不适用；本次没有修改图像处理或模型。
- 影响范围：`AGENTS.md`、`算法更新记录.md`。
- 已知限制：记录质量仍依赖每次更新时填写真实测试数据。
- 验证：检查Markdown结构和链接路径。

## 阶段20 · 2026-09-05：单件训练的严格实例筛选

- 目的：防止新采集单件帧中的手部、相邻物块或托盘深度碎片按“最大连通域”混入有标签训练集。
- 算法变化：`geometry-rgbd-train` 新增可选 `--strict-single-object`。开启时仅接受托盘 ROI 内恰有一个 RGB-D 分割实例的标注帧；检测到多个实例即拒绝并记录原因。默认行为仍保留最大 RGB-D 连通域选择，以保证旧模型可复现。
- 预期效果：减少错配标注特征对新训练候选的污染，同时不降低实时抓取安全门限。
- 实际效果：`pilot-03` 的 300 条新记录中，严格训练接受 254 个物块特征，拒绝 16 帧（2–8 个实例）。仅用新数据的独立候选在 `multi-02/03` 可临时核对的 55 个实例中为 42/55（76.36%）。以 v4 的289个样本为底座、追加这254个样本的增量候选共543个样本，为47/55（85.45%），与 v4 持平；两者均未超过 v4，因此未替换当前模型。
- 视觉审查：实际查看 `pilot-03` 的 27 帧 RGB/深度联系表，以及候选在 `multi-02/03` 的 20 帧联系表和 40 个物块裁剪。确认托盘 ROI 大多正确；`d415-000020755`、`d415-000019390`、`d415-000001745`、`d415-000002895`、`d415-000005104` 有多候选/碎片，五六棱柱与三棱柱仍有分类混淆。
- 影响范围：`geometry_rgbd_model.py`、`cli.py`、`test_geometry_models.py`、`RGBD数据采集与训练.md`；候选模型现归类在 `models/experimental/rgbd/`，包括未推广的`geometry-rgbd-v4-pilot03-strict-20260905.npz`，详细结果见[新数据视觉复审与严格训练报告_20260905.md](docs/reports/新数据视觉复审与严格训练报告_20260905.md)。
- 已知限制：严格筛选依赖当前分割结果，会拒绝深度断裂但真实只有一个物块的帧；它不是自动真值校验，也未解决多物块中的形状混淆。回放仍是开发批次，且 `multi-02` 第6–10帧标签未确认。
- 验证：新增严格筛选单测；`tests/test_geometry_models.py` 14项通过，`compileall`和`git diff --check`通过。没有启动相机或机械运动；只有 RGB-D 且 `PICKABLE`、`selected=true` 的结果才能驱动动作的规则不变。

## 阶段21 · 2026-09-05：空托盘参考约束异常ROI扩张

- 目的：新RGB-D画面中桌面和托盘边缘被并入工作区域，出现4个物块却检测出10–12件；直接加训练样本无法修复该分割错误。
- 算法变化：RGB-D标定保存可选托盘参考多边形；ROI扩张超过参考面积1.5倍才取交集，正常平移保持实时ROI；重合不足参考50%时安全拒绝并清除稳定计数。旧标定文件兼容。深度前景、三维几何/面拓扑和抓取门限不放宽。
- 预期效果：减少背景和托盘边缘误检，且避免固定像素边界裁掉正常移动的托盘物块。
- 实际效果：multi-04/05共31帧，数量匹配13→26帧，整帧形状组成完全匹配10→12帧，总候选171→116。83.87%仅为数量匹配率，不是形状准确率。旧multi-02/03临时核对仍47/55。固定交集原型虽然新帧数量匹配29/31，但旧场景裁切回归，未采用。
- 训练：495个物块清单（另55空托盘）；494可读RGB-D文件对无完全重复，1张旧RGB缺失。固定ROI试验接受425单实例，按215训练/124验证/86同次拍摄时间留出比较KNN；新候选未稳定胜过v4，未覆盖原模型。详细筛选、污染风险及口径见报告。阶段20的16个拒绝帧分母为270物块帧，不是300物块帧（另30空托盘）。
- 视觉审查：实际查看新多物块31帧、旧多物块20帧最终RGB/深度总览，以及pilot-03抽样27帧。multi-04首3帧桌面误检减少；第11/12/14帧仍有边缘碎片，multi-05第2/9帧仍重复分割。圆锥采集 `d415-000020755` 有手部干扰；仍有多面体unknown与边缘截断。
- 影响范围：`rgbd.py`、`pipeline3d.py`、`geometry_rgbd_model.py`；RGB-D测试、回放/组成评估/运行时特征调查脚本及使用文档。模型保持v4；新候选单独保存在`models/experimental/rgbd/`。见[深度识别优化报告_20260905.md](docs/reports/深度识别优化报告_20260905.md)。
- 已知限制：开发回放不是独立准确率；没有逐实例真值；正常范围内的轻度ROI扩张仍存在。旧标定没有参考字段时不会自动启用新约束；硬件位置明显变化需重新标定。本次未连接相机或机械运动。
- 验证：最终141项pytest通过；compileall和git diff --check通过。保存/加载兼容、正常平移、异常扩张、无深度拒绝和运动联锁测试通过。

## 阶段22 · 2026-09-06：三维几何与可见面拓扑的可选加权训练

- 目的：继续优化已有代码的形状混淆，检查可见面拓扑噪声与三维尺寸、深度分布等权计算距离的影响。
- 算法变化：`DepthGeometryModel.fit` 支持正特征权重，归一后折入NPZ缩放并重学距离拒识门限；省略时保持旧行为，兼容v3模型。新增训练坏输入检查及NaN/空特征明确拒识。候选基础20维权重1、拓扑权重0.25，保留真实深度使用。
- 预期效果：降低不稳定面拓扑对分类的影响，减少多面体误判与拒识；不是降低深度或抓取安全门限。
- 实际效果：同口径单件验证96/124→105/124，错判6→5；同次拍摄末段61/86→68/86。旧多场景临时已审实例47/55→50/55，但新多场景整帧组成12/31→10/31。因此候选不替换v4，阶段21已验证的ROI改进继续使用。不能把90.91%称为整体准确率。
- 数据：沿用固定ROI调查的425特征（215训练/124验证/86时间留出），候选继承289个v4特征共504样本。缓存RGB-D文件对无完全重复，但v4历史数据可能与旧验证集重叠；非独立泛化测试。新多场景31帧没有完全重复RGB-D文件对。
- 视觉审查：实际查看multi-04/05全部31帧、multi-02/03全部20帧以及pilot-03九类抽样27帧的RGB/深度联系表和旧multi-03的40个裁剪；确认部分旧六棱柱/三棱锥拒识改善，同时新场景有错误分类，手部、边界碎片、拆分和孔洞问题仍在。
- 影响范围：`geometry_rgbd_model.py`、`test_rgbd_dataset.py`、`probe_depth_metric.py`、`probe_depth_classification.py`及使用记录。实验模型`geometry-rgbd-weighted-candidate-20260906.npz`归类在`models/experimental/rgbd/`，没有覆盖原模型。见[深度加权分类试验报告_20260906.md](docs/reports/深度加权分类试验报告_20260906.md)。
- 已知限制：固定ROI训练特征与最终自适应路径存在差异；旧v4来源不完备；新多场景缺逐实例真值。RBF核分类和unknown填补仅作离线诊断，未部署。
- 验证：143项pytest通过，compileall和git diff --check通过；无相机/机械动作，原深度和运动安全约束不变。

## 阶段23 · 2026-09-08：RGB-D逐棱融合候选

- 目的：将二维弱棱线与真实深度面逐条对应，降低棱柱、棱锥等同色多面体的漏棱和形状混淆。
- 算法变化：增加深度面边界候选、低阈值Lab边缘、双侧带状平面拟合、跨平面残差、RGB/深度双向支持、线段合并和圆滑凸包过滤；追加9个融合特征及 `rgbd_geometry_v4_fused_edges` 模型格式。旧v1–v3模型仍走原路径。
- 预期效果：找回弱明暗棱，同时拒绝阴影、外轮廓、深度孔洞和曲面假棱，在不放宽安全门槛的前提下提升跨批次形状召回。
- 实际效果：403个严格单实例的嵌套留批评估中，旧特征259/403、宏召回64.51%，融合候选244/403、宏召回60.32%，下降4.19个百分点。multi-02/03临时已审实例47/55→42/55；multi-04/05整帧组成12/31→9/31，数量匹配维持26/31。P95耗时935.69→1036.08 ms，增加10.73%。候选未推广，当前仍使用v4。
- 视觉审查：查看pilot-01/02/03每类1帧共27帧，以及multi-02/03全部20帧、multi-04/05全部31帧RGB/深度联系表。最终抽样保留81条融合棱、拒绝108条RGB候选；圆锥假棱已抑制，但三棱柱与五/六棱柱弱棱仍有漏检，孔洞、边缘碎片和拆分继续影响输入。完整帧ID和发现见[RGB-D棱线融合试验报告_20260908.md](docs/reports/RGB-D棱线融合试验报告_20260908.md)。
- 影响范围：`face_topology3d.py`、`geometry_rgbd_model.py`、`rgbd_edge_audit.py`、CLI、训练/评估脚本、测试和使用文档；实验模型与输出均独立保存。
- 已知限制：没有逐棱人工端点真值；现有批次域差异大；多物体新批次只有组成标签。融合候选不得用于实际分拣，结果不是比赛准确率。
- 验证：148项pytest通过，compileall和git diff检查通过；没有连接相机或机械动作，只有RGB-D、`PICKABLE`且`selected=true`的结果才能驱动动作。

## 2026-09-08：工作空间与模型分级整理

- 算法变化：无。本次是目录、模型分级、历史产物归档和引用路径整理，不占用算法阶段编号。
- 整理结果：当前 RGB 开发模型与 RGB-D 稳定模型进入 `models/stable/`；未推广候选进入 `models/experimental/`；早期兼容模型进入 `models/archive/`。RGB 测试图集进入 `fixtures/rgb/`，说明文档进入 `docs/`，阶段21–23关键评估证据进入 `artifacts/evaluations/`。
- 分支：仓库只有 `main` 和其远端跟踪分支 `origin/main`，不存在需要合并或归档的其他代码分支。
- 历史输出：最终报告与联系表已迁出；其余可再生成的 `output` 内容因包含2934个历史已跟踪文件，采用可恢复的本地 `archive/generated-output-20260908/` 归档，没有直接永久删除。
- 安全：当前推荐模型保持 `models/stable/rgbd/geometry-rgbd-multipose-v4.npz`；实验模型不得驱动分拣，深度和运动互锁规则不变。

## 2026-09-08：补充 RGB-D 输出判读说明

- 目的：把检测框颜色、形状缩写、置信度、安全状态、`selected`、整帧健康、JSON 字段、棱线审查图颜色和评估口径集中记录，避免把状态色误读为分类真值，或把模型分数误读为实际准确率。
- 算法变化：无。本次仅新增和互链说明文档，不占用算法阶段编号，不修改模型、阈值、识别流程或安全互锁。
- 实际效果：新增[RGB-D结果颜色与字段说明](docs/guides/RGB-D结果颜色与字段说明.md)，并从主 README、数据训练说明、视觉审查流程和当前 v4 回放报告提供入口。
- 视觉审查：不适用；本次没有改变任何图像生成逻辑。文档颜色含义已逐项对照 `pipeline3d.py`、`rgbd_review.py` 和 `rgbd_edge_audit.py` 当前实现。
- 影响范围：文档和测试报告索引。
- 已知限制：显示颜色和诊断字段若以后修改，必须同步更新本说明；本文不能替代逐帧人工复审或真实机械抓取验收。
- 验证：检查 Markdown 链接、`git diff --check` 和文档变更差异；无代码变化，不重复运行算法测试。

## 2026-09-08：重构 README 导航并补充模块手册

- 目的：让首次使用者能从根 README 判断稳定/实验模型边界，按任务找到命令，并理解每个源码模块、配置、脚本和测试的职责。
- 算法变化：无。本次仅审查和补充项目文档，不占用算法阶段编号，不修改模型、特征、阈值、协议或安全互锁。
- 实际效果：根 README 新增当前推荐版本、阅读导航、任务入口、目录与模块分层；移除机器专用绝对路径；D415 示例显式传入已审配置和稳定 v4，避免省略模型时误以为已自动加载 v4。新增[模块使用说明](docs/guides/模块使用说明.md)，覆盖全部 `src/sorting_vision` 模块、33个 CLI 子命令、配置段、离线脚本、Python 集成和测试映射。
- 视觉审查：不适用；本次没有改变图像生成、分割、分类或标注逻辑。
- 影响范围：`README.md`、`docs/README.md`、`docs/guides/模块使用说明.md` 和本记录。
- 已知限制：源码、CLI 参数或模型推荐路径变化时必须同步更新手册；模块说明不替代函数类型提示、单元测试和阶段实测报告。
- 验证：逐项对照 `cli.py` 子命令和源码公共类/函数，检查文档内部链接与 `git diff --check`；无代码变化，不重复运行算法测试。

## 阶段24 · 2026-09-08：D435if 相机配置迁移

- 目的：把实时 RGB-D 输入从 Intel RealSense D415 切换到 D435if，同时避免继续用 D415 的采集参数、帧标识和已审结论描述新相机数据。
- 算法变化：将 RealSense 采集核心泛化为 `RealSenseSource`，新增 D435if 适配入口并保留 `RealSenseD415Source` 兼容类；相机型号、帧名前缀和 RGB/深度流分辨率改为配置驱动。新增 `config/d435if-initial.yaml`，初始使用 RGB/深度 1280×720@30、`d435if` 帧前缀和 280 mm 最小深度；原 D415 已审配置显式保留其旧参数，不覆盖历史。
- 预期效果：新采集文件和运行时元数据能够正确标明 D435if，以统一分辨率完成深度到彩色对齐，并为重新标定、重新采样和模型对照提供独立入口。
- 实际效果：软件定向回归 28 项通过，确认 D435if 配置可加载、参数能传入相机源、元数据和帧 ID 正确，旧 D415 类仍可运行。尚未连接实机，因此没有深度有效率、识别率、帧率或抓取成功率结果，不能宣称识别效果已经提升。
- 视觉审查：本次环境未连接 D435if，无法生成并实际检查新相机的 RGB/深度联系表；视觉迁移验收未完成。接入实机后必须检查托盘 ROI、对齐边缘、红外滤光下的深度孔洞、弱棱、反光、阴影和断裂，再记录具体批次与帧 ID。
- 影响范围：`camera.py`、`cli.py`、`config.py`、默认与 D435if/D415 配置、相机和批量采集测试、README 及 D435if 采集/迁移文档。现有 `geometry-rgbd-multipose-v4.npz` 仍是 D415 数据训练出的迁移基线，没有被覆盖或改名为 D435if 稳定模型。
- 已知限制：D435if IMU 当前未进入识别链路；1280×720@30 依赖 USB 3 带宽；D415 的内参、外参、空托盘背景、颜色阈值和准确率不可直接作为 D435if 验收结果。实机标定和新批次验证完成前，只能用于离线迁移测试，不能据此放宽 `PICKABLE`、`selected=true` 或运动互锁规则。
- 验证：D435if 相机、采集、RGB-D 与三维管线定向测试 28 项通过；全量 pytest 150 项通过，`compileall` 和 `git diff --check` 通过。

## 2026-09-08：拆分单目与双目工作空间

- 目的：保留当前单相机识别工程的完整状态，并为新增侧置辅助相机建立相互隔离、可独立演进的双目工作空间。
- 算法变化：无。本次仅调整工作空间目录并复制可复用工程基线，不占用算法阶段编号；尚未实现双相机同步、标定、配准、融合识别或抓取坐标变换。
- 整理结果：当前工程（包括未提交改动、本地数据、虚拟环境、历史产物和缓存）整体归入仓库根目录的 `单目/`；新建并列的 `双目/`，复制通用源码、配置、模型、脚本、测试、测试夹具及 Python 安装入口，作为后续双相机开发起点。Git 元数据保留在仓库根目录。
- 预期效果：避免双目试验直接干扰现有单目基线，并允许两套工作空间分别安装、测试和演进。
- 实际效果：完成目录存在性、关键源码/模型复制和 Python 编译检查；单目与双目基线分别通过全部 150 项 pytest 测试。本次未改变或评估识别率、时延和抓取效果。
- 视觉审查：不适用；没有修改图像处理、模型或数据，也没有生成新的识别结果。
- 影响范围：仓库目录布局及后续命令工作目录；原有相对路径需在 `单目/` 内执行，双目开发命令需在 `双目/` 内执行。
- 已知限制：`双目/` 当前只是复用基线，尚不能利用第二摄像头；两台相机的硬件触发/时间同步、内外参标定、遮挡互补策略、置信度融合和 RGB-D 运动安全链路均待后续设计。复制的模型仍来自既有单相机数据，不代表双目模型。原 `.venv` 文件已随单目工程保留，但 Windows 虚拟环境启动器含旧绝对路径，需在新目录重建后再日常使用。
- 安全：双目功能完成并验证前不得把侧置 RGB 或未经配准的结果变成可执行抓取；只有经过真实深度验证且满足 `status=PICKABLE`、`selected=true` 的结果才能驱动运动。

## 阶段25 · 2026-09-09：俯视 RGB-D 与侧视 RGB 的形状证据融合

- 目的：利用侧置普通 USB RGB 相机补充物块侧面轮廓和棱线，降低棱柱、棱锥等俯视轮廓相似类别的歧义，同时保持俯视 RGB-D 对实例、三维位姿和抓取安全的唯一控制权。
- 算法变化：新增双线程有界缓存和主机单调时钟帧配对，默认时差上限50 ms；新增两机 ChArUco 内外参标定、哈希与质量门禁；用俯视实例点云和料盘平面构造三维包围体并投影侧视 ROI，重叠超过30%判侧视遮挡；侧视空盘差分、面积、清晰度、填充率和可见率构成质量 `q`；分类器输出完整类别概率和拒识原因，并以侧视权重 `0.30×q` 做对数概率融合。双方概率均不低于0.75且冲突时拒识；融合需最高概率不低于0.72、前两类差不低于0.12；俯视距离拒识禁止升级，间隔拒识只可在前两候选中消歧。侧视异常时单目降级门槛为0.85。
- 预期效果：在比赛平台独立留出集上提高俯视易混类别的宏召回，同时不提高错误接受率、不增加安全状态绕过，并把投影、同步、侧视质量和冲突原因完整留在诊断中。
- 实际效果：软件全量回归为167项通过、1项按可选依赖跳过；单目合成回归仍检测12件并在稳定帧选择`black:cylinder`，健康状态`ok`。当前未连接双相机，也没有侧视训练模型、实拍标定或独立留出数据，因此尚无识别成功率提升、投影P95、同步P95或端到端P95实测，不能把预期提升描述为已实现。
- 视觉审查：实际打开用户提供的两张空盘 JPG 和一张临时平台 JPG，并查看生成的三联联系表。俯视空盘存在白色低对比和横向亮度带；侧视空盘背景含型材/工具且有明显阴影；临时平台扎带固定、外露线缆和横梁强边缘带来位移与遮挡风险。三张图缺深度、内参、同步时间和标定板，不能审查投影 ROI、深度孔洞或融合分类。具体文件与发现见[双视角融合实现与原型审查_20260909.md](docs/reports/双视角融合实现与原型审查_20260909.md)。
- 影响范围：`camera.py`、`dual_view.py`、`dual_validation.py`、`pipeline3d.py`、`classification3d.py`、`geometry_rgb.py`、`geometry_rgbd_model.py`、`interlock.py`、`server.py`、`cli.py`、配置、测试和双视角使用文档；未修改`单目/`和现有模型文件。
- 已知限制：侧视模型和两套平台标定文件尚未产生；软件同步不等于硬件触发；临时 JPG 不能作为正式标定；概率温度必须用比赛平台独立标定子集确定。未满足宏召回提升至少3个百分点、错误接受率不升、无安全升级、投影P95≤3 px、配对P95≤50 ms和端到端P95≤1 s之前，正式运行继续使用单目稳定链路。
- 验证：新增双视角定向测试16项，并扩展UVC固定成像控制与双路运动互锁测试；全量`167 passed, 1 skipped`，`compileall`和CLI帮助检查通过。JSON schema继续为v2；测试确认侧视证据不能把`DEPTH_INVALID`升级为`PICKABLE`。没有启动机械运动。

## 阶段26 · 2026-09-09：双相机同步拍摄与三 AprilTag 实时标定

- 目的：提供可直接在临时/比赛平台操作的双相机数据拍摄助手，并按“两个料盘对角固定标签、一个自由移动旋转标签”完成俯视 RGB-D 与侧视 RGB 的实时标定和可复核原始记录。俯视 RGB 与深度来自同一台 RGB-D 相机，不作为两台独立相机。
- 算法变化：新增三联实时拍摄界面、双视角静止/清晰度/深度有效率/同步质量门、空盘优先门和按平台/批次断点续拍；新增 AprilTag 36h11 检测、三标签打印资产、两固定标签料盘坐标估计、自由标签双视角姿态去重、两机内参与外参求解、尺度/极线质量验收，并仅在固定标签投影出的料盘多边形内拟合空盘深度平面。强制拍摄只记录质量覆盖标记，不改变运行时安全规则。
- 预期效果：减少双相机数据错配和模糊/运动样本，允许操作者用较小单标签覆盖两机画面完成标定，同时用固定对角标签稳定定义料盘原点、方向和尺度。
- 实际效果：合成测试中生成的 ID 0/1/2 均可被程序回读；合成针孔投影可恢复160×160 mm料盘中两个对角标签位置且尺度误差低于1%；新增程序定向测试6项通过，全量`173 passed, 1 skipped`。未连接真实双相机，尚无实机 RMS、投影 P95、同步 P95、时延或识别率结果。
- 视觉审查：实际查看生成的三标签布局预览和 ID 0 打印图；确认固定标签位于对角且同向、自由标签区分清楚、非比例警告可见，打印图黑白边界无灰阶插值且四周白色静区完整。ID 1/2 由同一路径生成并自动检测成功。未取得实机帧，反光、标签翘曲、深度孔洞和真实 ROI 尚未审查。详见[三AprilTag采集标定程序实现与审查_20260909.md](docs/reports/三AprilTag采集标定程序实现与审查_20260909.md)。
- 影响范围：`dual_capture_app.py`、`apriltag_calibration.py`、两个独立脚本、对应测试、README和双视角使用说明；只修改`双目/`，未修改`单目/`及现有模型。
- 已知限制：单平面小标签的内参精度高度依赖自由姿态覆盖，必须至少20对且包含画面四角、尺度和倾角变化；两个固定标签方向、中心内缩或黑框实测尺寸错误会使标定无效。软件同步不等于硬件触发，临时平台标定不能复制到比赛平台。三标签标定不替代机器人外参，侧视仍不能生成抓取坐标或绕过RGB-D安全拒识。
- 验证：AprilTag生成/检测、对角料盘坐标、双视角姿态多样性、采集质量门、三联渲染、保存/清单续拍测试通过；全量pytest、`compileall`、两个程序帮助命令和`git diff --check`通过。没有启动相机或机械运动。

## 阶段27 · 2026-09-09：Windows 双路实机稳定采集

- 目的：解决 D435if 俯视 RGB+深度与侧置 USB RGB 同时运行时出现的侧路黑屏、主路卡顿和 `Frame didn't arrive within 5000`，并用标定程序的真实采集路径完成持续负载验证。
- 根因：Windows“相机”和 RealSense Viewer 同开以及独立子程序基线均证明硬件可并发；故障来自 OpenCV/DirectShow 与 librealsense 同进程运行，以及 AprilTag 计算使串行程序不能持续排空 RealSense 帧流。侧相机 DirectShow 能力表中设备索引为 `0`，仅声明 `1280×720@30 YUY2/NV12`，未声明 MJPEG；索引 `2` 实际是 RealSense RGB。
- 算法变化：新增 `ProcessOpenCVCameraSource`，把侧置 DirectShow/NV12 采集隔离到独立进程，并通过容量为2的 JPEG 队列传递新帧、满队列丢弃过期周期；新增 `ThreadedRealSenseSource`，让 librealsense 的创建、等待帧和关闭始终归属于同一专用线程，并复制最新 RGB-D 帧供上层消费；双相机线程缓存持续运行，识别不再阻塞采集；配对时会等待侧路时间越过主帧，再在前后侧帧中选择最近者。两个独立采集/标定程序新增俯视RGB、原始深度、侧视RGB三种宽高覆盖参数，并分别支持主路与侧路帧率覆盖；非正整数会在命令行解析阶段拒绝。三AprilTag的两个固定ID和一个自由ID新增平台YAML默认值与命令行单次覆盖，生成/标定共用同一解析规则，并在开相机前检查非负、互异和字典范围。单目相机入口保持原行为。
- 实机结果：在俯视 RGB/深度 `640×480@15`、侧视 `1280×720@30 NV12` 下，每对帧均执行俯视和侧视 AprilTag 检测。核心路径连续120对达到15.06 FPS，配对误差P95为14.61 ms、最大39.24 ms、120/120同步；随后通过标定程序真实相机工厂复测120对达到15.08 FPS，配对误差P95为12.81 ms，119/120低于50 ms，唯一54.92 ms样本由`UNSYNCED`门禁拒绝。两次均无黑屏、无RealSense超时。
- 视觉审查：从标定程序真实相机工厂取得一组同步三联图，配对时差0.38 ms。俯视RGB、对齐深度和侧视RGB均有有效画面，两路RGB都能看到三个AprilTag，未出现侧路黑屏或重复读取RealSense RGB；深度与俯视料盘边框位置对应。仍可见白色料盘亮度渐变、侧视画面偏暗、料盘边缘深度孔洞和少量散点，这些属于后续照明、空盘平面和质量门要处理的现场问题，不能仅凭本次画面放宽标定验收阈值。
- 环境与配置：`temporary.yaml` 固定侧相机索引0、`DirectShow + NV12`、自动曝光/白平衡、进程隔离和线程配对。GStreamer不能增加驱动未声明的相机端MJPEG档位，当前方案不依赖GStreamer。诊断用`pygrabber/comtypes`已卸载，`pip check`无损坏依赖。
- 已知限制：进程间 JPEG 是传输实现，不等于相机输出 MJPEG；偶发超过50 ms的帧仍必须拒绝，不能用于标定。此次只验证采集、同步和AprilTag检测负载，尚未完成20姿态实拍标定、投影误差验收或双视角识别率对照，不能据此宣称融合识别率提升。
- 验证：自定义ID与标定配置定向测试28项通过；全量`182 passed`，`pip check`、`compileall`和`git diff --check`通过。未启动机械运动。

## 阶段28 · 2026-09-10：高分辨率实时配对去积压

- 目的：解决标定窗口两路 RGB 和深度均正常、AprilTag 也已检出，但窗口显示 `pair 344.4 ms` 并以 `cameras missing or unsynchronised` 拒绝固定参考的问题。
- 根因：俯视改为高分辨率后，AprilTag 检测速度低于采集速度；原配对器按顺序取最旧俯视帧，而侧视有界缓存已淘汰同一时刻的旧帧，于是旧俯视帧与较新的侧视帧配对，产生数百毫秒时差。截图中的绿色角点和三路有效画面证明本次拒绝不是标签 ID 错误、相机黑屏或深度缺失。
- 算法变化：实时双路读取改为总是弹出最新俯视 RGB-D 帧，并清除已被更新帧取代的俯视积压；仍等待侧路跨过俯视时间并选择最近侧帧，`50 ms` 同步安全门保持不变。新增突发帧回归测试，确认处理延迟后返回最新俯视帧而不是积压首帧。
- 预期效果：检测或预览短时变慢时不再持续扩大配对时差；过期帧被丢弃，标定记录保持实时性。真正超过 `50 ms` 的帧仍被拒绝，不能通过提高阈值绕过。
- 视觉审查：用户截图中俯视 RGB、对齐深度、侧视 RGB 都有有效画面，两个固定标签在两路 RGB 中均显示绿色检测框；唯一直接失败证据是 `pair 344.4 ms`。修复后尚未在被旧进程占用的实机上复测，必须退出并重启标定程序后观察窗口时差。
- 影响范围：双相机实时配对器、同步回归测试和三 AprilTag 使用说明；未修改标签几何、标定求解、融合阈值或抓取安全状态。
- 已知限制：丢弃积压帧可以恢复实时性，但不能解决 USB 物理带宽、驱动停帧或相机本身时钟抖动；高分辨率仍不稳定时应退回已验证的 `640×480@15 + 1280×720@30 NV12` 配置。
- 验证：双视角、相机和 AprilTag 程序定向测试 `40 passed`；全量回归 `183 passed`，`pip check`、`compileall` 和 `git diff --check` 通过。未启动机械运动。

## 阶段29 · 2026-09-10：固定俯视原厂内参与标定失败留窗

- 目的：解决用户完成20组自由标签姿态后按 `C`，程序以 `fixed AprilTag planes disagree by more than 10 degrees` 直接退出的问题，并防止低重投影误差掩盖不可信内参与畸变。
- 根因：旧实现用单个30 mm平面标签的20组图像重新自由拟合 D435if 彩色内参和五个畸变参数。首批近似平面移动数据曾产生 `k2=12.46、k3=-106.58`；改进姿态后的第二批仍把原厂约 `fx=1368 px` 重估为约 `1480 px`，将同一固定参考在原厂内参下 `2.75°` 的法向差放大为 `18.51°`。低 RMS 不能证明这种欠约束模型物理正确。
- 算法变化：俯视改为保留 RGB-D 帧携带的 RealSense 原厂彩色内参，以逐姿态 PnP 重投影 RMS 作为验证指标，不再从小型自由标签重估俯视内参与畸变；侧相机独立标定固定高阶 `k3`，降低小样本过拟合；固定标签法向错误包含实测角度；交互脚本捕获几何与 OpenCV 求解异常，在窗口和终端提示后继续运行，不再崩溃退出。新增 `--replay-session`，校验保存的标签 ID、参考图、深度、20组配对图和元数据后离线重算，避免求解失败后重复占用相机采集。
- 实拍回放：使用用户当前保存的20对 `1920×1080` 俯视 RGB、`1280×720` 侧视 RGB 与固定参考回放，修正后完整返回标定对象，不再触发法向异常。主/侧重投影 RMS 分别为 `0.393/0.219 px`；首次错误沿用 `160×160 mm、内缩20 mm` 时尺度误差为 `30.395%`。用户随后提供实测 `S=24、W=154、H=154、D=15 mm`，离线重算后尺度误差降至 `2.288%`，但共视投影 P95 仍为 `7.302 px`；两项仍分别超过1%和3 px门槛，因此正确保持 `valid=false`，没有推广为可用标定。
- 视觉审查：实际查看 `fixed-reference-primary.png`、俯视 `pose-000.png` 和侧视 `pose-019.png`。两个固定标签平贴于白色料盘对角，自由 ID 50 已手持并出现明显倾斜；两路均存在亮度梯度，侧视手部和强阴影占据较大区域。固定参考清晰，但当前料盘实际尺寸明显不能由默认160 mm代替；自由标签采集仍需每次停稳后记录，避免软件同步内的手部运动转化为共视误差。
- 影响范围：三 AprilTag 标定核心、交互失败处理、保存 session 离线回放、定向测试与使用说明；没有修改融合分类、安全拒识或机器人运动接口。
- 已知限制：`CameraIntrinsics` 当前不保存 RealSense 畸变模型及系数，俯视暂以原厂针孔参数和零畸变参与 OpenCV 求解；用户仍须提供料盘宽高、固定标签中心内缩和标签黑框的实测毫米值。当前实拍共视 P95 和尺度误差未达门槛，不能宣称标定成功。
- 验证：新增固定内参重投影、法向错误诊断和保存 session 回放测试，AprilTag/双视角定向回归 `33 passed`；全量回归 `187 passed`，`pip check`、`compileall` 和 `git diff --check` 通过。未启动机械运动。
