# 项目结构与 D435IF 配置变更说明（2026-09-09）

## 目录名称

外层工作区已由 `工训赛` 更名为 `tech_`。当前两个项目目录为：

- `monocular camera/`：单目项目。
- `stereo camera/`：双视角项目；D435IF 主相机与侧置 UVC 相机相关代码、配置和文档均在此目录。

所有双目安装、测试、采集和标定命令都必须在 `tech_\stereo camera`（包含 `pyproject.toml`）中运行。

```powershell
Set-Location "C:\Users\d114\Desktop\tech_\stereo camera"
```

## 虚拟环境

重命名目录后，旧 `.venv` 中记录的绝对 Python 路径已失效，不能继续使用。需要在各自项目目录重新创建独立环境：

```powershell
py -3 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev,realsense,dual]"
.\.venv\Scripts\python.exe -m pytest -q
```

`monocular camera` 和 `stereo camera` 的包名均为 `sorting-vision`，不得共用同一个虚拟环境。

若 pytest 报 `PermissionError: ... Temp\\pytest-of-d114`，说明系统临时目录没有当前账户的访问权限；不代表测试代码失败。改用项目内临时目录：

```powershell
.\.venv\Scripts\python.exe -m pytest -q --basetemp .pytest-tmp
```

## 当前 D435IF 取流配置

已通过 RealSense SDK 实测当前连接设备：

- 设备：`RealSense D435IF`
- 序列号：`332322072879`
- 彩色流：`1920×1080 @ 30 FPS`
- 深度流：`848×480 @ 30 FPS`

该组合已完成 SDK 协商测试；此前使用的 `1280×720` RGB/深度默认值不再作为当前设备的默认配置。

已同步更新以下配置：

- `stereo camera/config/d435if-initial.yaml`
- `stereo camera/config/default.yaml`

示例采集命令见：

- `stereo camera/docs/guides/D435if多物体批量测试拍摄.md`
- `stereo camera/docs/guides/D435if数据集拍摄助手.md`

## 安全与验证范围

本次变更只确认了相机流配置可以启动，不代表识别模型、深度阈值、抓取面或机械臂外参已针对 D435IF 完成验收。D415 训练的模型仍仅可作迁移对照；在新的 D435IF 实拍数据、视觉复审和独立验证通过前，不得用于真实抓取。
