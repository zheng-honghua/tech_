# 六棱柱识别优化说明

## 问题定位

逐物块检查 `multi-03` 后，原 `geometry-rgbd-multipose-v4.npz` 对有效的 9 个六棱柱姿态识别正确 8 个，召回率为 88.9%。误判帧是 `frame-02`：六棱柱平放后，深度轮廓接近矩形，模型将其判为正八面体。

同时发现训练器曾在 1920×1080 原图提取特征，而实时流水线使用 `processing_scale=0.75`。这会使轮廓和可见面拓扑的尺度不一致。现在训练和实时处理共用 `resize_rgbd_frame()`，彩色图、深度图及相机内参同步缩放。追加训练可通过 `geometry-rgbd-train --base-model` 保留旧模型样本。

## 最终识别逻辑

保留原 v4 模型作为主分类器，并增加受限的三维几何复核：

1. 仅当模型预测为 `octahedron` 时启动复核。
2. 计算物块 X/Y 公制跨度和最靠近相机的 2 mm 平面占比。
3. 当短跨度不小于 35 mm、长跨度不小于 42 mm、近平面占比不小于 0.35 时，改判为 `hexagonal_prism`。

这些门限来自当前实物数据：正八面体训练样本的主跨度最大为 32.96 mm，而六棱柱可达到 67.15 mm。规则不会仅根据二维颜色或固定拍摄位置改判。诊断字段新增 `near_surface_ratio_2mm` 和 `metric_hexagonal_override`。

## 验证结果

- 六棱柱：8/9（88.9%）提升到 9/9（100%）。
- 第 9、10 帧留出姿态：2/2 正确。
- 正八面体门限范围与六棱柱不重叠。
- 最终 20 帧标注图已人工检查；托盘外物体未进入最终结果。
- 失败的全局 ROI 扩张方案已撤销，因为会引入托盘边框伪目标。

当前继续使用：

```powershell
.\.venv\Scripts\python.exe -m sorting_vision.cli camera-live `
  --source realsense `
  --color-width 1920 --color-height 1080 `
  --depth-width 640 --depth-height 480 --fps 30 `
  --rgbd-shape-model models/geometry-rgbd-multipose-v4.npz
```

本结果仍属于同批次测试。若更换物块尺寸，需要重新标定公制范围，不能直接沿用当前复核门限。
